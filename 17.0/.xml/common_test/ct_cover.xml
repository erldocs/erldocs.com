<?xml version="1.0" encoding="utf8" ?>
<!DOCTYPE erlref SYSTEM "erlref.dtd">
<erlref>
<header>
<title>ct_cover</title>
<prepared></prepared>
<responsible></responsible>
<docno>1</docno>
<approved></approved>
<checked></checked>
<date></date>
<rev>A</rev>
<file>ct_cover.xml</file></header>
<module>ct_cover</module>
<modulesummary>Common Test Framework code coverage support module.</modulesummary>
<description>
<p>Common Test Framework code coverage support module.</p>
  
   <p>This module exports help functions for performing code
      coverage analysis.</p></description>
<funcs>
<func>
<name>add_nodes(Nodes) -&gt; {ok, StartedNodes} | {error, Reason}</name>
<fsummary>Add nodes to current cover test (only works if cover support
        is active!).</fsummary>
<type>
<v>Nodes = [atom()]</v><v>StartedNodes = [atom()]</v><v>Reason = cover_not_running | not_main_node</v></type>
<desc><marker id="add_nodes-1"/>

<p>Add nodes to current cover test (only works if cover support
        is active!). To have effect, this function should be called
        from init_per_suite/1 before any actual tests are performed.
  </p>
</desc></func>
<func>
<name>remove_nodes(Nodes) -&gt; ok | {error, Reason}</name>
<fsummary>Remove nodes from current cover test.</fsummary>
<type>
<v>Nodes = [atom()]</v><v>Reason = cover_not_running | not_main_node</v></type>
<desc><marker id="remove_nodes-1"/>

<p>Remove nodes from current cover test. Call this function
        to stop cover test on nodes previously added with add_nodes/1.
        Results on the remote node are transferred to the Common Test
        node.
  </p>
</desc></func>
<func>
<name>cross_cover_analyse(Level, Tests) -&gt; ok</name>
<fsummary>Accumulate cover results over multiple tests.</fsummary>
<type>
<v>Level = overview | details</v><v>Tests = [{Tag, Dir}]</v><v>Tag = atom()</v><v>Dir = string()</v></type>
<desc><marker id="cross_cover_analyse-2"/>

<p>Accumulate cover results over multiple tests.
        See the chapter about <seealso marker="cover_chapter#cross_cover">cross cover
        analysis</seealso> in the users's guide.
  </p>
</desc></func></funcs>

<authors>
<aname> </aname>
<email> </email></authors></erlref>