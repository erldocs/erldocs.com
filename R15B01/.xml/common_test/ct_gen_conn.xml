<?xml version="1.0" encoding="latin1" ?>
<!DOCTYPE erlref SYSTEM "erlref.dtd">
<erlref>
<header>
<title>ct_gen_conn</title>
<prepared></prepared>
<responsible></responsible>
<docno>1</docno>
<approved></approved>
<checked></checked>
<date></date>
<rev>A</rev>
<file>ct_gen_conn.xml</file></header>
<module>ct_gen_conn</module>
<modulesummary>Generic connection owner process.</modulesummary>
<description>
<p>Generic connection owner process.
  </p></description>
<section><title>DATA TYPES</title><marker id="types"/>

<taglist>
<tag><c>handle() = pid()</c></tag>
<item><marker id="type-handle"/>
<p>A handle for using a connection implemented
   with ct_gen_conn.erl.</p>
</item>
</taglist></section>
<funcs>
<func>
<name>start(Name, Address, InitData, CallbackMod) -&gt; {ok, Handle} | {error, Reason}</name>
<fsummary>Open a connection and start the generic connection owner process.</fsummary>
<type>
<v>Name = term()</v><v>CallbackMod = atom()</v><v>InitData = term()</v><v>Address = term()</v></type>
<desc><marker id="start-4"/>

<p>Open a connection and start the generic connection owner process.</p>
  
   <p>The <c>CallbackMod</c> is a specific callback module for
   each type of connection (e.g. telnet, ftp,...). It must export the
   function <c>init/3</c> which takes the arguments
   <c>Name</c>, <c>Addresse</c>) and
   <c>InitData</c> and returna
   <c>{ok,ConnectionPid,State}</c> or
   <c>{error,Reason}</c>.</p>
</desc></func>
<func>
<name>stop(Pid::Handle) -&gt; ok</name>
<fsummary>Close the telnet connection and stop the process managing it.</fsummary>
<type>
<v>Handle = handle()</v></type>
<desc><marker id="stop-1"/>

<p>Close the telnet connection and stop the process managing it.</p>
</desc></func>
<func>
<name>do_within_time(Fun, Timeout) -&gt; FunResult | {error, Reason}</name>
<fsummary>Execute a function within a limited time (tool-internal use only).</fsummary>
<type>
<v>Fun = function()</v><v>Timeout = integer()</v></type>
<desc><marker id="do_within_time-2"/>

<p>Execute a function within a limited time (tool-internal use only).</p>
  
   <p>Execute the given <c>Fun</c>, but interrupt if it takes
   more than <c>Timeout</c> milliseconds.</p>
  
   <p>The execution is also interrupted if the connection is
   closed.</p>
</desc></func>
<func>
<name>call(Pid, Msg) -&gt; term()
</name>
<fsummary> </fsummary>

<desc><marker id="call-2"/>
 </desc></func></funcs>

<authors>
<aname> </aname>
<email> </email></authors></erlref>